-- COMPLETE EXAMPLE WITH ALL FEATURES
-- Load the library
local ProfessionalUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/fluxScript82/nixame/main/source.lua"))()

-- Create notifications
ProfessionalUI:CreateNotification({
    Title = "Welcome!",
    Description = "Professional UI v3.0 Enhanced is loading...",
    Type = "Info",
    Duration = 3
})

-- Create watermark
local watermark = ProfessionalUI:CreateWatermark({
    Text = "Professional UI v3.0 Enhanced | " .. Player.Name,
    Theme = "Dark"
})

-- Create keybinds
ProfessionalUI:CreateKeybind({
    Name = "Toggle UI",
    Key = Enum.KeyCode.RightControl,
    Callback = function()
        print("UI toggled via keybind!")
        ProfessionalUI:CreateNotification({
            Title = "Keybind",
            Description = "UI toggled!",
            Type = "Success"
        })
    end
})

-- Create key system with enhanced features
local keySystem = ProfessionalUI:CreateKeySystem({
    Title = "Professional UI v3.0 Enhanced",
    Description = "Enter your premium access key to unlock all advanced features",
    Key = "ProfessionalUI_Enhanced_2024",
    KeyLink = "https://example.com/getkey-enhanced",
    Theme = "Dark",
    Callback = function()
        print("🚀 Access granted! Loading enhanced UI...")
        
        -- Create main window with all features
        local Window = ProfessionalUI:CreateWindow({
            Title = "Professional UI v3.0 Enhanced Edition",
            Size = Vector2.new(750, 550),
            Theme = "Dark",
            Draggable = true,
            MinimizeToTray = true,
            SaveConfig = true
        })
        
        -- Main Tab with all elements
        local MainTab = Window:CreateTab({
            Name = "🏠 Main",
            Icon = "🏠"
        })
        
        MainTab:CreateLabel({
            Text = "🎉 Welcome to Professional UI v3.0 Enhanced!",
            Size = 16,
            Color = Window.Theme.Primary
        })
        
        MainTab:CreateSeparator({Text = "Basic Elements"})
        
        MainTab:CreateButton({
            Text = "🚀 Launch Feature",
            Description = "Click to launch the main feature",
            Callback = function()
                ProfessionalUI:CreateNotification({
                    Title = "Feature Launched",
                    Description = "Main feature has been activated!",
                    Type = "Success"
                })
            end
        })
        
        MainTab:CreateToggle({
            Text = "Auto Farm",
            Description = "Automatically farm resources",
            Default = false,
            Callback = function(value)
                print("Auto Farm:", value)
            end
        })
        
        MainTab:CreateSlider({
            Text = "Walk Speed",
            Description = "Adjust your character's walking speed",
            Min = 16,
            Max = 100,
            Default = 16,
            Increment = 1,
            Callback = function(value)
                if game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("Humanoid") then
                    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = value
                end
            end
        })
        
        MainTab:CreateDropdown({
            Text = "Teleport Location",
            Description = "Select a location to teleport to",
            Options = {"Spawn", "Shop", "Boss Arena", "Secret Area", "VIP Room"},
            Default = "Spawn",
            Callback = function(option)
                print("Teleporting to:", option)
                ProfessionalUI:CreateNotification({
                    Title = "Teleport",
                    Description = "Teleported to " .. option,
                    Type = "Info"
                })
            end
        })
        
        MainTab:CreateTextbox({
            Text = "Player Name",
            Description = "Enter a player name to target",
            PlaceholderText = "Enter player name...",
            Default = "",
            Callback = function(text)
                print("Target player:", text)
            end
        })
        
        MainTab:CreateColorPicker({
            Text = "Theme Color",
            Description = "Choose your preferred theme color",
            Default = Color3.fromRGB(88, 101, 242),
            Callback = function(color)
                print("Selected color:", color)
            end
        })
        
        MainTab:CreateProgressBar({
            Text = "Loading Progress",
            Description = "Shows the current loading progress",
            Min = 0,
            Max = 100,
            Default = 75
        })
        
        -- Combat Tab
        local CombatTab = Window:CreateTab({
            Name = "⚔️ Combat",
            Icon = "⚔️"
        })
        
        CombatTab:CreateSeparator({Text = "Combat Settings"})
        
        CombatTab:CreateToggle({
            Text = "Auto Attack",
            Description = "Automatically attack nearby enemies",
            Default = false,
            Callback = function(value)
                print("Auto Attack:", value)
            end
        })
        
        CombatTab:CreateToggle({
            Text = "Auto Block",
            Description = "Automatically block incoming attacks",
            Default = false,
            Callback = function(value)
                print("Auto Block:", value)
            end
        })
        
        CombatTab:CreateSlider({
            Text = "Attack Range",
            Description = "Set the maximum attack range",
            Min = 5,
            Max = 50,
            Default = 15,
            Callback = function(value)
                print("Attack Range:", value)
            end
        })
        
        CombatTab:CreateDropdown({
            Text = "Combat Style",
            Description = "Choose your combat approach",
            Options = {"Aggressive", "Defensive", "Balanced", "Stealth"},
            Default = "Balanced",
            Callback = function(option)
                print("Combat Style:", option)
            end
        })
        
        -- Settings Tab
        local SettingsTab = Window:CreateTab({
            Name = "⚙️ Settings",
            Icon = "⚙️"
        })
        
        SettingsTab:CreateSeparator({Text = "UI Settings"})
        
        SettingsTab:CreateToggle({
            Text = "Show Notifications",
            Description = "Enable or disable notification popups",
            Default = true,
            Callback = function(value)
                print("Show Notifications:", value)
            end
        })
        
        SettingsTab:CreateSlider({
            Text = "UI Scale",
            Description = "Adjust the overall UI scale",
            Min = 50,
            Max = 150,
            Default = 100,
            Callback = function(value)
                print("UI Scale:", value)
            end
        })
        
        SettingsTab:CreateDropdown({
            Text = "Theme",
            Description = "Select your preferred UI theme",
            Options = {"Dark", "Light", "Purple", "Ocean", "Neon"},
            Default = "Dark",
            Callback = function(option)
                print("Theme changed to:", option)
                ProfessionalUI:CreateNotification({
                    Title = "Theme Changed",
                    Description = "UI theme changed to " .. option,
                    Type = "Info"
                })
            end
        })
        
        SettingsTab:CreateButton({
            Text = "💾 Save Config",
            Description = "Save current settings to file",
            Callback = function()
                Window:SaveConfig("my_settings")
            end
        })
        
        SettingsTab:CreateButton({
            Text = "📁 Load Config",
            Description = "Load settings from file",
            Callback = function()
                Window:LoadConfig("my_settings")
            end
        })
        
        -- Info Tab
        local InfoTab = Window:CreateTab({
            Name = "ℹ️ Info",
            Icon = "ℹ️"
        })
        
        InfoTab:CreateLabel({
            Text = "Professional UI Library v3.0 Enhanced",
            Size = 18,
            Color = Window.Theme.Primary
        })
        
        InfoTab:CreateSeparator()
        
        InfoTab:CreateLabel({
            Text = "✨ Features:",
            Size = 14
        })
        
        InfoTab:CreateLabel({
            Text = "• Enhanced Key System with animations",
            Size = 12
        })
        
        InfoTab:CreateLabel({
            Text = "• Notification System with toast messages",
            Size = 12
        })
        
        InfoTab:CreateLabel({
            Text = "• Keybind System for hotkeys",
            Size = 12
        })
        
        InfoTab:CreateLabel({
            Text = "• Config Save/Load System",
            Size = 12
        })
        
        InfoTab:CreateLabel({
            Text = "• Multiple UI Elements (Sliders, Dropdowns, etc.)",
            Size = 12
        })
        
        InfoTab:CreateLabel({
            Text = "• Sound Effects and Smooth Animations",
            Size = 12
        })
        
        InfoTab:CreateLabel({
            Text = "• 5 Built-in Themes",
            Size = 12
        })
        
        InfoTab:CreateSeparator()
        
        InfoTab:CreateButton({
            Text = "🎉 Test All Features",
            Description = "Demonstrates all UI features",
            Callback = function()
                -- Test notifications
                for i, type in ipairs({"Info", "Success", "Warning", "Error"}) do
                    spawn(function()
                        wait(i * 0.5)
                        ProfessionalUI:CreateNotification({
                            Title = type .. " Test",
                            Description = "This is a " .. type:lower() .. " notification!",
                            Type = type,
                            Duration = 3
                        })
                    end)
                end
                
                print("🎉 All features tested!")
            end
        })
        
        print("✅ Professional UI v3.0 Enhanced loaded successfully!")
        print("🎮 Press Right Control to toggle UI via keybind")
        
        ProfessionalUI:CreateNotification({
            Title = "Success!",
            Description = "Professional UI v3.0 Enhanced loaded successfully!",
            Type = "Success",
            Duration = 5
        })
    end
})

-- Alternative: Skip key system for testing
--[[
local Window = ProfessionalUI:CreateWindow({
    Title = "Professional UI v3.0 Enhanced - Direct Access",
    Size = Vector2.new(700, 500),
    Theme = "Neon", -- Try different themes!
    Draggable = true,
    MinimizeToTray = true
})

local TestTab = Window:CreateTab({Name = "🧪 Test"})
TestTab:CreateButton({Text = "Test All Features", Callback = function()
    print("Testing all features!")
end})
--]]
